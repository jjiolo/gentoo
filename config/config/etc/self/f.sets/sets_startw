
#############
cat << EOF

ROOT
----
su -l -s /bin/sh -c 'SEATD_VTBOUND=7 setsid seatd </dev/null &>/dev/null & exit' desktop
chown desktop:toor /dev/shm/seatd.sock
chmod 0660 /dev/shm/seatd.sock

^ none root without /dev/tty0 permissions
  leaks all keystrokes to underlying tty
  alt+left alt+left poweroff in foot kills the computer

ROOT
----
seatd -u toor -g toor

^ works but runs the xorg server process as root

TOOR
----
XDG_RUNTIME_DIR=/dev/shm WLR_RENDERER=pixman setsid sway </dev/null &>/dev/null

EOF
#############

