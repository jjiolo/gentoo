#!/bin/sh
#############

mountpoint -q "${mount}" && umount "${mount}"

[ -b "/dev/mapper/integrity" ] && integritysetup close integrity
[ -b "/dev/mapper/crypt" ] && cryptsetup close crypt

#############

lsblk

#############
