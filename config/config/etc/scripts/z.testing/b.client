
#############
pkill -x /usr/sbin/httpd
pkill -x p2pd
pkill -x sshd
#############

[ -z "${1}" ] || {

sleep 3
#############
ash /etc/services/httpd/start
ash /etc/services/p2pd/start
sshd -r /tmp/storage/.ssh/sshd </dev/null &>/dev/null
#############

}

