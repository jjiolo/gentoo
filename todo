
#############

openssl-3.1.0 < - not beta anymore released 14th march

#############

laptop kernel can function without usb-hid...
^ no external keyboard + mouse
  no ability to plug and play usb payloads...
  cheapy usbguard (grsec lock bus sysctl toggles)

#############

 git did the thing where it needs to stash and pop ?
 ^ hasnt for ages find out what triggers it...

#############

 gcc build just rm'd /etc/portage symlink

#############

 renice +20 -u 250 < - doesnt work without portage on host
 portage:x:250:250::/tmp/storage/stage3:/bin/ash
 portage:x:250:
 portage:!:::::::

#############

gcc glibc died because aliases...
^ didnt update properly ?
  linux-headers glibc gcc build order ?
  probably my bad ? (would only clean build on preinstalled stage3)

#############

* make i.server flush terminal screen then print...

* ebuild ? (doh server/client + zoneforwarding + ip/hostname blacklist)
  tinyproxy http/https
  dnsdist
  github.com/dnscrypt/doh-server
  github.com/LinkTed/doh-client

^ only the large tar pit dinosaurs implemented it...
  needs to be small modern and 5 seconds to configure
  because its based on secure standards not configurable 90s

* sec-keys/openpgp-keys-gentoo-{auth,release,developers}
   sec-keys/openpgp-keys-gentoo-release-20230329
    curl -o /usr/share/openpgp-keys/gentoo-release.asc https://dev.gentoo.org/~mgorny/dist/openpgp-keys/gentoo-release.asc.${PV}.gz
   sec-keys/openpgp-keys-gentoo-auth-20230329
    curl -o /usr/share/openpgp-keys/gentoo-auth.asc https://dev.gentoo.org/~mgorny/dist/openpgp-keys/gentoo-auth.asc.${PV}.gz"
   sec-keys/openpgp-keys-gentoo-developers-20230515
    curl -o /usr/share/openpgp-keys/gentoo-developers.asc https://qa-reports.gentoo.org/output/active-devs.gpg

* hashes - verify vfat (inter mittens usb.mount errors)
^ vfat/ext4 usb pairs... < - add integritysetup to ext4 + hashes

#############

 two types of ldd
  runtime
  during ? < - need to find out how to list the second type of lib include...
  ^ then stop fucking up ebuild creation using ldd only ?

#############

